services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: vidforge
      POSTGRES_PASSWORD: vidforge
      POSTGRES_DB: vidforge
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

volumes:
  postgres-data:
